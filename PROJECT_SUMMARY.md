# Agent 全栈开发项目经验总结（简历版）

## 项目 Summary

主导跨境电商 Agent Workspace 的全栈研发，面向运营、老板及开发人员构建统一的 AI 工作台。项目以对话式任务为入口，围绕抖音关键词研究、Temu 商品上架、社媒发布等高频运营场景，搭建 React 前端、FastAPI 服务、LangGraph Agent Runtime、Skill/MCP 能力插件和 Electron 桌面端。通过将 LLM 分析能力、外部黑盒自动化服务与本地浏览器 Worker 解耦，形成从任务发起、参数确认、异步执行、进度追踪、异常处理到结构化报告和管理看板的完整闭环，并为后续接入更多跨境电商工具提供标准化扩展架构。

## 核心职责与成果

- **构建 Agent 全栈工作台与多角色体验**：使用 React + TypeScript 搭建 Codex 风格 Workspace，支持对话式 Composer、任务列表、任务详情、结构化报告、工具状态页及老板经营看板；通过业务视图/开发视图分层，让运营关注结论与行动建议，让开发人员追踪 MCP 调用、执行进度、错误及预算状态。

- **设计 LangGraph 驱动的可观测 Agent Runtime**：围绕任务初始化、Skill 路由、宏观计划、步骤执行、状态观察、失败收敛、报告生成与 Schema 校验构建状态机；统一维护任务状态、执行事件、工具输出、质量分数、循环预算和错误分类，使异步任务可恢复、可轮询、可定位问题。

- **沉淀 Skill + MCP 的可扩展业务架构**：将领域能力拆分为 Skill 输入/输出 Schema、流程计划、提示词和工具注册配置；通过 MCP Runtime 统一管理服务发现、权限校验、参数映射、工具调用和结果标准化。针对 Temu 上架、社媒发布等已有成熟引擎，采用黑盒 Job 包装，避免在平台内重复实现外部自动化链路。

- **解决浏览器登录态自动化无法安全云端化的问题**：设计并实现 MeatWorker Windows 本地执行器，将蝉妈妈等依赖浏览器 Cookie、扫码登录和本机环境的自动化采集放在闲置 PC 执行；通过心跳、任务领取、完成回传、Token 鉴权和异常状态机制与云端平台解耦，实现“平台负责任务调度，本地负责有状态浏览器执行”的可靠架构。

- **优化 LLM 使用边界、稳定性与成本**：建立 heavy/light 分层模型路由和会话级模型钉扎机制，复杂经营分析使用重模型，摘要、分类、缺参追问和失败说明使用轻模型；明确 Excel 解析、浏览器自动化、状态轮询、结构化成功失败判断等固定流程全部由代码和 MCP 处理，避免 LLM 幻觉、无效循环和不必要调用。

- **打通数据到经营决策的报告闭环**：将抖音采集的关键词、视频/商品侧数据、热度、竞争信息、查询来源和错误诊断归一化，结合规则兜底与 LLM 分析生成关键词分层、优先级、证据、风险提示与行动建议；进一步通过老板端聚合 API 将任务级报告转化为机会点、待处理事项和经营摘要。

- **完成多端交付与工程化保障**：基于 FastAPI 提供任务、上传、MCP 配置、工具健康检查和报告接口；使用 Docker Compose 处理 API/MCP 网关共享文件目录，解决 Excel 与媒体文件跨服务访问问题；使用 Electron 打包 Windows/macOS 桌面端，并通过 pytest、ruff、mypy 和 GitHub Actions 覆盖核心任务、模型路由、Worker 队列、报告适配和工具状态能力。

## 技术栈

`Python`、`FastAPI`、`LangGraph`、`LangChain`、`React`、`TypeScript`、`Electron`、`MCP`、`JSON Schema`、`Playwright`、`PyInstaller`、`Docker Compose`、`pytest`、`GitHub Actions`。
