# LLM — NewAPI OpenAI-compatible；路由见 AGENTS.md「模型路由」
OPENAI_API_BASE=https://api.hyhacct.com/v1
# heavy（复杂运营分析 / 方案撰写）；OPENAI_API_KEY / LLM_MODEL 为兼容别名
OPENAI_API_KEY=sk-...
LLM_MODEL=gpt-5.6-luna
LLM_HEAVY_API_KEY=
LLM_HEAVY_MODEL=gpt-5.6-luna
# light（摘要 / 事实提取 / 意图分类 / 记忆压缩 / 简单格式化）
LLM_LIGHT_API_KEY=sk-...
LLM_LIGHT_MODEL=agnes-2.0-flash

# LangSmith (optional, production observability)
# LANGCHAIN_TRACING_V2=true
# LANGCHAIN_API_KEY=lsv2_...
# LANGCHAIN_PROJECT=agent-platform

# Checkpoint DB (production: use Postgres)
# CHECKPOINT_DB_URL=postgresql://user:pass@localhost:5432/agent_platform

# MCP servers config path
MCP_CONFIG_PATH=config/mcp.json
AGENT_ENV=dev
MCP_RUNTIME_ENABLED=true
# Leave unset to derive from AGENT_ENV (dev=true, staging/prod=false)
# MCP_ALLOW_STUB_FALLBACK=true
# MCP_WRITE_TOKEN=
# 蝉妈妈个人版采集（Playwright 持久化 profile；禁止把 Cookie 写入仓库）
# 本地：python scripts/chanmama_login.py
# DOUYIN_CHROME_USER_DATA_DIR=
# CHANMAMA_HEADED=0
#
# 抖音肉机 Worker（出站领任务；见 docs/wip/douyin-meat-machine-mcp.md）
# 推荐：python scripts/_fill_douyin_worker_env.py 生成 Token；DEPLOY_PASS 需人工填 SSH 密码
# DOUYIN_WORKER_URL=https://www.yoto.work/platform-mcp
# DOUYIN_WORKER_TOKEN=
# DOUYIN_WORKER_ID=肉机
# DOUYIN_WORKER_POLL_S=3
# 同机双启（Temu Agent 可选路径；勿把抖音采集合并进 Agent）：
# COMMANDER_AGENT_EXE=D:\dev\workspace\commander-agent-t260220-main\commander-agent-t260220-main\build\bin\Agent.exe
# 一键：scripts\start-meat-hands.bat  ·  状态：python scripts\meat_hands_status.py
# DEPLOY_HOST=124.223.27.98
# DEPLOY_USER=root
# DEPLOY_PASS=
# 服务器 platform_mcp 侧（与 Worker Token 一致，由 deploy 脚本写入远端）：
# DOUYIN_JOB_DIR=/data/douyin-jobs
# DOUYIN_JOB_TIMEOUT_S=240
#
# 一键构建 EXE：scripts\build-meat-worker.bat → dist\meat-worker\
# 运维一页：docs\wip\douyin-meat-ops-runbook.md · docs\wip\meat-worker-exe.md

# Platform MCP gateway runs on deploy host (not laptop). Client URL:
# MCP_GATEWAY_URL=https://www.yoto.work/platform-mcp/mcp
# (config/mcp.json platform_mcp.url should match)
#
# Excel uploads MUST share the gateway-readable absolute path (API + MCP 同机):
# UPLOAD_ROOT=/data/platform-mcp/uploads
# Prod API: https://www.yoto.work/agent-platform/api/docs

# Commander Temu Job（API 工具状态探测 + MCP 网关共用）
COMMANDER_API_BASE=https://www.yoto.work/api/v1
COMMANDER_ACCESS_TOKEN=
COMMANDER_DEFAULT_AGENT_ID=肉机
COMMANDER_DEFAULT_PLATFORM=temu
# COMMANDER_SHOP_MAP={"徐铨金牌店":"8381218"}

# Automedia 社媒发布（MCP 网关）
SOCIAL_UPLOAD_API_BASE=https://automedia.yoto.work
SOCIAL_UPLOAD_TOKEN=

# Vector store (optional)
# CHROMA_PERSIST_DIR=knowledge/index/chroma

# BOSS read-only Douyin operating reports (API only; no browser/client exposure)
BOSS_REPORTS_DB_HOST=epay-mysql
BOSS_REPORTS_DB_PORT=3306
BOSS_REPORTS_DB_NAME=douyin_reports
BOSS_REPORTS_DB_USER=boss_report_reader
BOSS_REPORTS_DB_PASSWORD=
BOSS_REPORTS_DB_CONNECT_TIMEOUT_S=5
