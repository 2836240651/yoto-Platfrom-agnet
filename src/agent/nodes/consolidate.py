"""Consolidate task results into long-term memory."""

from __future__ import annotations

from agent.state import AgentState


def consolidate(state: AgentState) -> dict:
    """Placeholder: write episodic summary + ingest reports to vector KB."""
    return {
        "status": "done",
        "progress_step_name": "完成",
        "progress_percent": 100,
    }
