# 渔具全品类知识库

本目录提供可审计的渔具分类、标准词、常见别名与窄变体，用于抖音词分析任务的上游查询规划。

- 精确输入词永远优先查询。
- 知识库只追加最多两个标准词/窄变体，并在诊断和结果卡中保留 `queried_term`、`query_level`、`query_source=fishing_gear_kb`、`query_dimension`。
- 不将小众词静默桥接到 `钓鱼`、`渔具`、`鱼竿` 等大类词。
- 该目录是品类能力索引，不代表未经产品目录或 SKU 验证的现货承诺。
