{
  "name": "tuodiao-workspace-desktop",
  "version": "0.1.2",
  "private": true,
  "description": "TUODIAO Workspace desktop shell (bundled web + remote API)",
  "main": "main.cjs",
  "author": "TUODIAO",
  "license": "UNLICENSED",
  "scripts": {
    "start": "electron .",
    "pack": "electron-builder --dir",
    "dist": "electron-builder --win",
    "build:web": "npm --prefix ../web run build && node ./scripts/copy-web-dist.cjs"
  },
  "build": {
    "appId": "work.tuodiao.workspace",
    "productName": "TUODIAO",
    "copyright": "Copyright TUODIAO",
    "directories": {
      "output": "dist-electron"
    },
    "asar": true,
    "files": [
      "main.cjs",
      "preload.cjs",
      "package.json",
      "web-dist/**/*"
    ],
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": ["x64"]
        },
        {
          "target": "portable",
          "arch": ["x64"]
        }
      ],
      "artifactName": "TUODIAO-${version}-${os}-${arch}.${ext}"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "TUODIAO"
    },
    "portable": {
      "artifactName": "TUODIAO-${version}-portable.exe"
    },
    "mac": {
      "target": [
        {
          "target": "dmg",
          "arch": ["x64", "arm64"]
        },
        {
          "target": "zip",
          "arch": ["x64", "arm64"]
        }
      ],
      "artifactName": "TUODIAO-${version}-mac-${arch}.${ext}"
    }
  },
  "devDependencies": {
    "electron": "^37.2.0",
    "electron-builder": "^26.0.12"
  }
}
